/*package com.adobe.aem.guides.demo.core.schedulers;

	import org.osgi.service.metatype.annotations.AttributeDefinition;
	import org.osgi.service.metatype.annotations.ObjectClassDefinition;

	@ObjectClassDefinition(name = "Page Publisher Scheduler Config")
	public @interface Task8Configuration {

	    @AttributeDefinition(name = "Enable Scheduler")
	    public boolean enable() default true;

	    @AttributeDefinition(name = "Scheduler Name")
	    public String scheduler_name() default "page.publisher.scheduler";

	    @AttributeDefinition(name = "Cron Expression")*/
	   // public String scheduler_expression() default "*/10 * * ? * *";  // every 10 seconds

/*	    @AttributeDefinition(name = "Page Root Path")
	    public String page_root_path() default "/content/Demo/us/en/page1";
	}
*/

